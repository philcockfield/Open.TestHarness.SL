<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    xmlns:dg="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Data">

    <SolidColorBrush x:Key="RowGridline" Color="#e5e5e5" />
    <SolidColorBrush x:Key="ColumnGridline" Color="#f3f3f3" />

    <Style x:Key="SimpleGrid" TargetType="dg:DataGrid">
        <Setter Property="AutoGenerateColumns" Value="false" />
        <Setter Property="AlternatingRowBackground" Value="Transparent" />
        <Setter Property="RowBackground" Value="Transparent" />
        <Setter Property="SelectionMode" Value="Single" />
        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource RowGridline}" />
        <Setter Property="VerticalGridLinesBrush" Value="{StaticResource ColumnGridline}" />
        <Setter Property="GridLinesVisibility" Value="Horizontal, Vertical" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="HorizontalScrollBarVisibility" Value="Hidden" />
    </Style>

</ResourceDictionary>